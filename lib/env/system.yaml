env:

    system:

        start:
            type: foo

        load:
            type: num
            sqlite: init

        name:
            type: str

        diskfree:
            name: Freier Speicher des Laufwerks auf dem Smarthome.py derzeit lauft
            type: num

        disksize:
            name: Groesse des Laufwerks auf dem Smarthome.py derzeit lauft
            type: num

        diskusage:
            name: Genutzte Groesse des Laufwerks auf dem Smarthome.py derzeit lauft
            type: num

        diskusagepercent:
            name: Genutzte Groesse des Laufwerks auf dem Smarthome.py derzeit lauft in Prozent der Gesamtgroesse
            type: num

        libs:

            ephem_version:
                name: Installierte Version ephem
                type: str
